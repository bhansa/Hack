import time 
start_time = time.time()

#code
import itertools
power_set = [1,2,3]
def findsubsets(S,m):
	return set(iterators.combinations(S,m))
#using combinators and iterators


dead = time.time() - start_time